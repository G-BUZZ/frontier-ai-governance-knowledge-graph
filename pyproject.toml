[project]
name = "frontier-ai-governance-knowledge-graph"
version = "0.1.0"
description = "Knowledge graph for frontier AI governance, model evaluations, benchmarks and safety frameworks."
requires-python = ">=3.13"

dependencies = [
    "networkx",
    "pandas",
    "matplotlib",
    "pyyaml"
]
